class Solution {
  public:
    // Function to check if a string is a palindrome.
    bool isPalindrome(string& s) {
        
        string rev=s;
       reverse(rev.begin(),rev.end());
       if(s==rev)
       {
           return true;
       }
       else
       {
           return false;
       }
    }
};
